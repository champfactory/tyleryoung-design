---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import { essayGroups } from '../data/essayGroups';
import { stories } from '../data/stories';

const essays = await getCollection('essays');
const count = essays.length;
const words = ['zero','one','two','three','four','five','six','seven','eight','nine','ten'];
const spelled = words[count] ?? String(count);
const bySlug = new Map(essays.map((e) => [e.data.slug, e]));

/** Which story each essay feeds, so the index shows the connection both ways. */
const storyFor = new Map<string, (typeof stories)[number]>();
for (const s of stories) for (const slug of s.leadership) storyFor.set(slug, s);
---

<BaseLayout
  title="Leadership essays | Tyler Young"
  description="Seven instruments for building design organizations — scoring models, complexity budgets, thresholds and ladders that made judgment explicit enough to survive without the person who made it."
  descriptionDraft
  current="essays"
>
  <section class="shell page">
    <p class="label">Leadership writing</p>
    <h1 class="display">{spelled.charAt(0).toUpperCase() + spelled.slice(1)} instruments for building design organizations</h1>
    <p class="lede">
      Each of these is the same move the work on the homepage makes: take a judgment
      that lived in someone's head and make it explicit enough to survive without
      them. Written years before the thing carrying the judgment was a machine.
    </p>

    <div class="groups">
      {
        essayGroups.map((group, i) => (
          <section class="group" aria-labelledby={`g${i}`}>
            <div class="group-head">
              <span class="num">{String(i + 1).padStart(2, '0')}</span>
              <div>
                <p class="eyebrow">{group.eyebrow}</p>
                <h2 class="heading" id={`g${i}`}>
                  {group.title}
                </h2>
                {group.blurb && <p class="blurb">{group.blurb}</p>}
              </div>
            </div>

            <ul>
              {group.slugs.map((slug) => {
                const e = bySlug.get(slug);
                const s = storyFor.get(slug);
                return e ? (
                  <li>
                    <a class="link" href={`/${slug}`}>
                      {e.data.title}
                    </a>
                    {s && (
                      <span class="feeds">
                        feeds <a href={s.slug}>{s.eyebrow}</a>
                      </span>
                    )}
                  </li>
                ) : null;
              })}
            </ul>
          </section>
        ))
      }
    </div>
  </section>
</BaseLayout>

<style>
  .page {
    padding-top: 88px;
  }

  h1 {
    font-size: clamp(32px, 5.2vw, 52px);
    margin: 16px 0 0;
    max-width: 20ch;
  }

  .lede {
    margin: 26px 0 0;
    max-width: 56ch;
    font-size: 19px;
    line-height: 1.6;
    color: var(--color-ink-2);
  }

  .groups {
    margin-top: 64px;
    display: grid;
    gap: 52px;
  }

  .group-head {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 18px;
    padding-top: 20px;
  }

  .num {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.09em;
    color: var(--color-signal);
    margin-top: 6px;
  }

  .eyebrow {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    color: var(--color-ink-3);
    margin: 0 0 6px;
  }

  h2 {
    font-size: 26px;
    margin: 0;
  }

  .blurb {
    margin: 12px 0 0;
    font-size: 16px;
    line-height: 1.55;
    color: var(--color-ink-3);
    max-width: 34ch;
  }

  ul {
    list-style: none;
    margin: 20px 0 0;
    padding: 0;
    display: grid;
    gap: 16px;
  }

  li {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 6px 14px;
  }

  li a.link {
    font-size: 20px;
    line-height: 1.4;
  }

  .feeds {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    color: var(--color-ink-3);
    white-space: nowrap;
  }

  .feeds a {
    color: var(--color-signal);
    text-decoration: none;
    border-bottom: 1px solid currentColor;
  }

  @media (min-width: 810px) {
    .group {
      display: grid;
      grid-template-columns: minmax(0, 300px) minmax(0, 1fr);
      gap: 40px;
    }

    .group-head {
      border-top: 0;
      padding-top: 0;
    }

    .group {
      padding-top: 20px;
    }

    ul {
      margin-top: 0;
    }
  }
</style>
