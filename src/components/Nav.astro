---
const { current } = Astro.props as { current?: string };
---

<header class="nav">
  <div class="shell inner">
    <a class="wordmark" href="/">
      <span class="name">Tyler Young</span>
      <span class="role">Senior Director of Design</span>
    </a>
    <nav aria-label="Main">
      {/* One sentence, not a row of tabs. The connective words are plain text, so
          only the three things you can actually go and do are underlined. */}
      <p class="statement">
        I <a href="/how-i-design-with-ai" aria-current={current === 'building' ? 'page' : undefined}
          >design with AI</a
        > and <a href="/essays" aria-current={current === 'essays' ? 'page' : undefined}
          >lead teams</a
        >. <a href="mailto:43tylers@gmail.com">43tylers@gmail.com <span aria-hidden="true">→</span></a>
      </p>
    </nav>
  </div>
</header>

<style>
  .nav {
    /* Solid, not the translucent paper this used to be. A sticky header tinted
       88% paper let the page show through; a flat colour at partial opacity over
       scrolling content turns muddy, and the point of the field is that it is
       flat. */
    background: var(--color-paper);
    position: sticky;
    top: 0;
    z-index: 20;
  }

  .inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    padding-block: 14px;
  }

  .wordmark {
    display: flex;
    align-items: baseline;
    gap: 10px;
    text-decoration: none;
    color: var(--color-ink);
    flex-wrap: wrap;
  }

  .name {
    font-family: var(--font-mono);
    font-size: 15px;
    font-weight: 600;
    letter-spacing: 0.015em;
  }

  .role {
    font-family: var(--font-mono);
    font-size: 15px;
    font-weight: 400;
    letter-spacing: 0.015em;
    color: var(--color-ink);
  }

  ul {
    display: flex;
    gap: 22px;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  /* Sentence case, because it is a sentence — the uppercase treatment belonged
     to tabs. The type sits on `nav` rather than on the paragraph's class: as a
     class it lost to `nav a` for the links and left the connective words
     inheriting 19px body sans, so the sentence rendered at two sizes. */
  nav {
    font-family: var(--font-mono);
    font-size: 15px;
    font-weight: 500;
    letter-spacing: 0.015em;
    color: var(--color-ink);
  }

  /* Wraps on a narrow screen. Held on one line it is wider than a 390px
     viewport, which pushed the whole document sideways — the header cannot be
     the reason a page scrolls horizontally. */
  .statement {
    margin: 0;
  }

  @media (min-width: 760px) {
    .statement {
      white-space: nowrap;
    }
  }

  nav a {
    font: inherit;
    letter-spacing: inherit;
    text-decoration: none;
    color: var(--color-ink);
    padding-bottom: 2px;
    /* Links are blue and unadorned; the page you are on reads in ink. */
    color: var(--color-signal);
    transition: opacity 120ms ease;
  }

  nav a:hover,
  nav a:focus-visible {
    opacity: 0.75;
  }

  nav a[aria-current='page'] {
    color: var(--color-ink);
  }

  .inner {
    min-width: 0;
  }

  .wordmark,
  nav {
    min-width: 0;
  }

  /* At 390px the wordmark + three links measure wider than the viewport, which
     pushes the whole document's scrollWidth past the screen. The role line goes
     first (it's a nicety, not information), then the type tightens. */
  @media (max-width: 560px) {
    .role {
      display: none;
    }

    .name {
      font-size: 15px;
      letter-spacing: 0.015em;
    }

    nav a {
      font-size: 15px;
      letter-spacing: 0.015em;
    }

    ul {
      gap: 13px;
    }

    .inner {
      gap: 12px;
    }
  }
</style>
