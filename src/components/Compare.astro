---
/**
 * A note, and what it did.
 *
 * The feedback line sits above the pair so the causal chain is unmissable:
 * someone asked for this, and here is the before and after. Stacked rather than
 * side by side because these are wide, short strips of interface — side by side
 * would halve them to illegibility.
 */
interface Props {
  /** the reviewer's note, verbatim from FEEDBACK.md */
  note: string;
  before: string;
  after: string;
  beforeAlt: string;
  afterAlt: string;
  /** what to look at, if the change is easy to miss */
  tell?: string;
  /** the kind of decision this note produced */
  label?: string;
  /** one example carries the argument; it gets the room to. */
  emphasis?: boolean;
}

const { note, before, after, beforeAlt, afterAlt, tell, label, emphasis = false } = Astro.props;
---

<figure class:list={['compare', { emphasis }]}>
  {label && <p class="decision">{label}</p>}
  <p class="note">
    <span class="box" aria-hidden="true">☐</span>
    {note}
  </p>

  <div class="pair">
    <div class="side">
      <p class="tag">Before</p>
      <img src={before} alt={beforeAlt} loading="lazy" decoding="async" />
    </div>
    <div class="side">
      <p class="tag after">After</p>
      <img src={after} alt={afterAlt} loading="lazy" decoding="async" />
    </div>
  </div>

  {tell && <figcaption>{tell}</figcaption>}
</figure>

<style>
  .compare {
    margin: 0 0 40px;
    border: 1px solid var(--color-rule);
    background: var(--color-paper-raised);
  }

  /* The second example is the one that proves the page's argument, so it is the
     one given weight — a heavier frame, not a different component. */
  .compare.emphasis {
    border-width: 2px;
    border-color: var(--color-ink);
  }

  .decision {
    margin: 0 !important;
    padding: 11px 16px;
    font-family: var(--font-mono) !important;
    font-size: 10.5px !important;
    font-weight: 600;
    letter-spacing: 0;
    color: var(--color-signal) !important;
    border-bottom: 1px solid var(--color-rule);
  }

  .note {
    margin: 0 !important;
    padding: 14px 16px;
    display: flex;
    gap: 10px;
    font-family: var(--font-mono) !important;
    font-size: 13px !important;
    line-height: 1.6 !important;
    letter-spacing: 0.01em;
    color: var(--color-ink) !important;
    background: var(--color-paper-sunk);
  }

  .box {
    color: var(--color-ink-3);
    flex: none;
  }

  .pair {
    display: grid;
  }

  .side {
    padding: 16px;
  }

  .side + .side {
    border-top: 1px solid var(--color-rule);
  }

  .tag {
    margin: 0 0 10px !important;
    font-family: var(--font-mono) !important;
    font-size: 10px !important;
    font-weight: 600;
    letter-spacing: 0;
    color: var(--color-ink-3) !important;
  }

  .tag.after {
    color: var(--color-signal) !important;
  }

  img {
    display: block;
    width: 100%;
    height: auto;
    border: 1px solid var(--color-rule);
  }

  figcaption {
    font-family: var(--font-mono);
    font-size: 13px;
    line-height: 1.7;
    letter-spacing: 0;
    color: var(--color-ink-3);
    padding: 13px 16px;
    border-top: 1px solid var(--color-rule);
  }
</style>
